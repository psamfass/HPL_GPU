name: C/C++ CI

on:
  push:
    branches: [ "psamfass-ci" ]
  pull_request:
    branches: [ "psamfass-ci" ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
